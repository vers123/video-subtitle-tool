# 视频字幕提取工具 - Python 依赖清单
# 安装命令: pip install -r requirements.txt

# 语音转文字
openai-whisper>=20231117

# PyTorch（Whisper 依赖）
# GPU 加速（NVIDIA 显卡，推荐）：pip install torch --index-url https://download.pytorch.org/whl/cu126
# 仅 CPU：pip install torch
torch>=2.0.0

# ffmpeg Python 封装
ffmpeg-python>=0.2.0

# OCR 识别硬字幕（兼容 2.x 和 3.x）
paddleocr>=2.7.0
paddlepaddle>=2.6.0

# 图片预处理（灰度化、二值化，用于增强 OCR 识别率）
opencv-python>=4.8.0
numpy>=1.24.0

# SRT 字幕文件读写
pysrt>=1.1.2

# 命令行参数解析（标准库，无需安装）
# argparse

# 日志记录（标准库，无需安装）
# logging
